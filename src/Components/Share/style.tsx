import styled from 'styled-components'

const StyledShare = styled.div`
    display: flex;
    justify-content: space-evenly;
    padding: 30px 0;

    & button {
        border: none;
        background: none;
        outline: none;
        cursor: pointer;
    }
`
export { StyledShare }
