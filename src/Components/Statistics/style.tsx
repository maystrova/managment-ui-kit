import styled from 'styled-components'

const StyledStatistics = styled.div`
    display: flex;
    padding: 15px;
`

const StyledStatisticsTitle = styled.div`
    font-size: 0.8em;
    color: #878787;
`

const StyledStatisticsCompleted = styled.div`
    margin-right: 20px;
`

const StyledStatisticsCount = styled.div`
    font-size: 20px;
    font-weight: bold;
`

export {
    StyledStatistics,
    StyledStatisticsCompleted,
    StyledStatisticsCount,
    StyledStatisticsTitle,
}
